let affine(a, b, x: u32) : u32 = a * x + b
let transform = affine(4, 3)
let divmod(a, b: u32) : (u32, u32) = do return a / b, a % b end
let consume(a, b: u32) : u32 = do
  let q, r = divmod(a, b)
  return q * b + r
end
return { functions = { transform, divmod, consume } }
